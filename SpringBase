侵入式与非侵入式：
    侵入式是指：使代码对框架进行依赖
    非侵入式是指：代码没有对框架进行过多依赖

DI机制是指
    依赖注入和控制反转

spring的IOC(控制反转)的理解：
    ioc是指不需要自己创建对象，全交给容器进行创建

IOC的三种注入方法
    注解注入 构造函数注入 set注入

内存的可见性：
    指一个线程修改了共享数据，其他线程也能看见

指令重排序：
    指内存操作速度远慢于CPU运行速度,可能会发生后面的代码先执行，然后在硬件层面，
    CPU会将接收到的一批指令按照其规则进行重排序
    
HashMap：
    容量 * 负荷系数 = 阀值
容量是指hashMap的长度
负荷系数默认是0.75
阀值是如果到达了某种程度容量会扩大

Mybatis和Hibernate的区别
    首先它们都是由配置文件生成sqlSessionFactory，然后由sqlSessionFactory生成sqlSession对象，用来操作sql语句的

    hibernate可以自己生成sql语句，而mybatis需要手动生成
    hibernate通过自己的映射关系，大大降低了与数据库的耦合性，
    而mybatis与数据库的耦合性是根据sql语句来决定
    hibernate的配置比较复杂，mybatis配置比较简单
对象关系模型：

